# Helm Values Notes

These values files capture the operational intent for third-party platform components. They are not vendored charts; pair them with the approved upstream chart repositories in Argo CD or Helmfile and pin versions during promotion.

